#ifndef TIMES_TABLE_H
#define TIMES_TABLE_H

#include <stdlib.h>

void print_times_table(unsigned char ** const, const size_t);

unsigned short grade_times_table(unsigned char ** const, const size_t);

void print_grades(unsigned char * const, const size_t);

#endif
